# ─────────────────────────────────────────────────────────────
# Nginx Reverse Proxy Config for TrackCodex EC2 Backend
# Place this at /etc/nginx/sites-available/trackcodex
# Then: sudo ln -s /etc/nginx/sites-available/trackcodex /etc/nginx/sites-enabled/
# ─────────────────────────────────────────────────────────────

# Rate limiting zone
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;

# ─── HTTP (redirects to HTTPS after certbot) ──────────────────
server {
    listen 80;
    server_name api.trackcodex.com;

    # Certbot will add HTTPS redirect here automatically
    location / {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;

        # WebSocket / Socket.io support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    300s;   # Long for streaming/terminal

        # Rate limiting
        limit_req zone=api_limit burst=50 nodelay;
    }
}

# ─── IDE Server (ide.trackcodex.com) ───────────────────────────
server {
    listen 80;
    server_name ide.trackcodex.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;

        # WebSocket / Socket.io support (Critical for IDE)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for IDE
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    3600s; # Very long for active coding sessions
    }
}

# ─── HTTPS (auto-filled by Certbot if used) ──────────────────
# Run: sudo certbot --nginx -d api.trackcodex.com -d ide.trackcodex.com
